# Empty on purpose. Its presence at the project root anchors pytest's
# rootdir here, so `access_risk_analyzer` is importable in tests
# regardless of whether you run `pytest`, `python -m pytest`, or run it
# from a different working directory.
